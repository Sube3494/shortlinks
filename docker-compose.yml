version: '3.8'

services:
  shortlink:
    build: .
    container_name: shortlink-app
    restart: unless-stopped
    environment:
      - BASE_URL=https://shortlinks.sube.top
      - API_KEY=${API_KEY:-}  # 设置API密钥，留空则不启用认证
    ports:
      - "18000:8000"
    volumes:
      - ./data:/app/data

